numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.30

# برای برچسب فارسی نمودارها. matplotlib حروف فارسی را به هم نمی‌چسباند و
# ترتیب راست‌به‌چپ را رعایت نمی‌کند؛ این دو کتابخانه همان دو کار را انجام
# می‌دهند. اگر نصب نباشند، ماژول plots خودش به برچسب انگلیسی برمی‌گردد و
# آموزش بدون خطا ادامه پیدا می‌کند.
arabic-reshaper>=3.0
python-bidi>=0.4
