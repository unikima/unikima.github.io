"""
تنظیمات مرکزی پروژه.
همه‌ی پارامترهای قابل تنظیم در همین یک فایل جمع شده‌اند تا تغییر آنها
نیاز به دست‌کاری کد مدل‌ها نداشته باشد.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = ROOT / "data"
ARTIFACT_DIR = ROOT / "artifacts"
RESULTS_DIR = ROOT / "results"

for _d in (ARTIFACT_DIR, RESULTS_DIR):
    _d.mkdir(exist_ok=True)

# ---------- پیش‌پردازش ----------
MIN_RATINGS_PER_USER = 20   # حذف کاربران کم‌تعامل
MIN_RATINGS_PER_BOOK = 20   # حذف کتاب‌های کم‌بازخورد
TEST_FRACTION = 0.20        # سهم داده‌ی آزمون از امتیازهای هر کاربر
VALID_FRACTION = 0.10       # سهم اعتبارسنجی — برای تنظیم پارامتر، جدا از آزمون
RANDOM_SEED = 42

# آستانه‌ی «مرتبط بودن» برای معیارهای رتبه‌بندی
RELEVANCE_THRESHOLD = 4.0

# ---------- مدل شباهت آیتم‌محور ----------
ITEM_KNN_K = 40             # تعداد همسایه‌های هر کتاب
ITEM_KNN_SHRINKAGE = 25.0   # ضریب کوچک‌سازی برای شباهت‌های کم‌پشتوانه

# ---------- تجزیه ماتریس ----------
MF_FACTORS = 48
MF_EPOCHS = 18
MF_LR = 0.006
MF_REG = 0.04
MF_BATCH = 8_192

# ---------- PureSVD (موتور رتبه‌بندی) ----------
PURE_SVD_FACTORS = 128

# ---------- مدل محتوامحور ----------
CONTENT_MAX_FEATURES = 40_000
CONTENT_TOP_TAGS = 12       # تعداد برچسب‌های پرتکرار هر کتاب

# ---------- مدل ترکیبی ----------
HYBRID_ALPHA = 0.75         # وزن بخش مشارکتی؛ (1-alpha) وزن بخش محتوامحور

# ---------- ارزیابی ----------
TOP_K = 10
EVAL_USER_SAMPLE = 3_000    # نمونه کاربران برای معیارهای رتبه‌بندی
POPULARITY_PRIOR = 50.0     # ضریب هموارسازی بیزی در مدل پایه
N_BOOTSTRAP = 1_000         # تعداد بازنمونه‌گیری برای بازه اطمینان

# ---------- جاروب پارامتر ----------
# نمونه‌ی کوچک‌تر برای جاروب، چون هر مقدار آلفا یک ارزیابی کامل لازم دارد
TUNING_USER_SAMPLE = 1_000
ALPHA_GRID = [0.0, 0.15, 0.3, 0.45, 0.6, 0.7, 0.75, 0.8, 0.9, 1.0]
FACTOR_GRID = [16, 32, 64, 128, 192, 256]
